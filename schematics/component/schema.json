{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicLayeredComponent",
  "title": "Angular Component Options with Layer Wrapper",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": ["name"]
}
